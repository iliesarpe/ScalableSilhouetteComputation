# This file has moved to the repository root (image.def).
#
# Build from the repo root:
#   apptainer build image.sif image.def
#
# The root image.def does not bake source code into the container; cppCode/ is
# bind-mounted at runtime so Python scripts can be edited without rebuilding.
